clearvars
close all
clc

load nonlienarity.mat;

setupDroneEnv;
tello=getnelloDrone();
tello.connect();
tello.takeoff();

Te = 0.2; % période d'échantillonage
Vect_Pos = []
Start = tic;
while (FD.isRunning)
    if mod(tac(Start),Te) == 0
        % Retrieve the current position of the drone
        position = tello.getPosition();

    end
end 
